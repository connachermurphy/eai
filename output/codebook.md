# Codebook

Variable definitions for the generated files in this directory. This file is written by the pipeline scripts; edit the definitions in the generating script, not here.

<!-- BEGIN CODEBOOK SECTION: oews_panel -->
## OEWS year panel

Generated by `pipeline/panel.py`.

### `oews_panel.csv`

Stacked OEWS national detailed occupations for 2021-2024, with numeric fields coerced (suppression markers become NA).

| Variable | Definition |
| --- | --- |
| `occ_code` | Six-digit SOC 2018 occupation code. |
| `occ_title` | SOC occupation title. |
| `tot_emp` | Estimated total employment. |
| `emp_prse` | Percent relative standard error of employment. |
| `a_mean` | Mean annual wage. |
| `mean_prse` | Percent relative standard error of the mean wage. |
| `a_pct10 / a_pct25 / a_median / a_pct75 / a_pct90` | Annual wage percentiles. |
| `year` | OEWS reference year (May estimates). |
| `balanced_panel` | True when the occupation code appears in every year of the panel. |

<!-- END CODEBOOK SECTION: oews_panel -->

<!-- BEGIN CODEBOOK SECTION: occupational_characteristics -->
## Occupational characteristics panels

Generated by `pipeline/occupational_characteristics.py`.

The default run uses May 2022 OEWS. Runs with `--oews-year`/`--output-tag` write the same columns to tagged file names (e.g., `occupations_aei_oews_2024.csv`).

### `occupations_eloundou_et_al.csv`

SOC 2018 panel with Eloundou et al. (2023, 'GPTs are GPTs') occupation-level exposure scores and OEWS employment and wage fields.

| Variable | Definition |
| --- | --- |
| `soc_2018` | Six-digit SOC 2018 occupation code. |
| `title_2018` | SOC 2018 occupation title. |
| `group_id` | Connected-component ID of the bipartite SOC 2010<->2018 crosswalk graph; non-singleton groups mark one-to-many or many-to-many crosswalk relationships. |
| `dv_rating_alpha / dv_rating_beta / dv_rating_gamma` | Model-generated (GPT-4) exposure ratings from Eloundou et al., averaged from 8-digit O*NET-SOC codes to six-digit SOC 2018. alpha counts only direct LLM exposure (E1); beta adds half weight on exposure via LLM-powered software (E1 + 0.5*E2); gamma adds full weight (E1 + E2). |
| `human_rating_alpha / human_rating_beta / human_rating_gamma` | Human-annotator exposure ratings from Eloundou et al.; same alpha/beta/gamma aggregations and O*NET-SOC averaging as the dv_rating columns. |
| `oews_occ_title` | OEWS national occupation title for the matched code. |
| `oews_tot_emp` | Raw OEWS national total employment for the exact SOC 2018 code (NA when missing or suppressed). |
| `oews_a_mean` | OEWS annual mean wage. |
| `oews_a_median` | OEWS annual median wage. |
| `oews_soc_2018_broad` | SOC 2018 broad code (fifth digit zeroed) used for the employment fallback; NA for exact matches. |
| `oews_tot_emp_adjusted` | Employment after the broad-code fallback: the exact-match value, or the broad code's employment split equally across its detailed child codes without their own OEWS row. |
| `oews_broad_match` | True when employment came from the broad-code fallback rather than an exact SOC 2018 match. |
| `oews_tot_emp_imputed` | oews_tot_emp_adjusted with missing employment filled with the median across occupations, only for occupations that have exposure ratings. |
| `oews_emp_was_imputed` | True when the occupation has exposure ratings but no OEWS employment, so oews_tot_emp_imputed holds the median-employment fill value. |

### `occupations_aei.csv`

SOC 2010 panel of AEI task-level usage apportioned to occupations via O*NET task statements, with OEWS employment allocated across the SOC 2010<->2018 crosswalk. Count columns follow the pattern `{weight}_{platform}_{measure}`.

| Variable | Definition |
| --- | --- |
| `soc_2010` | Six-digit SOC 2010 occupation code (8-digit O*NET-SOC 2010 codes truncated to the parent SOC code). |
| `title_2010` | SOC 2010 occupation title. |
| `group_id` | Connected-component ID of the bipartite SOC 2010<->2018 crosswalk graph; non-singleton groups mark one-to-many or many-to-many crosswalk relationships. |
| `oews_tot_emp_allocated` | OEWS SOC 2018 employment (after the broad-code adjustment) allocated to SOC 2010 by splitting each SOC 2018 code's employment equally across its SOC 2010 crosswalk partners, then summing by SOC 2010. NA when no crosswalk partner has OEWS employment. |
| `oews_tot_emp_imputed` | oews_tot_emp_allocated with missing values filled with the median allocated employment across SOC 2010 occupations. Used as the employment weight and the per-capita denominator. |
| `oews_emp_was_imputed` | True when oews_tot_emp_allocated was missing and oews_tot_emp_imputed holds the median-employment fill value. |
| `oews_a_mean` | OEWS annual mean wage, averaged across the occupation's SOC 2018 crosswalk partners weighted by allocated employment. |
| `equal_1p_api_task_count` | AEI conversation count for the occupation's tasks; first-party API traffic; each task's counts split equally across the occupations sharing the task (weight 1/n occupations). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `equal_1p_api_automation_count` | AEI conversations in collaboration modes classed as automation (directive + feedback loop); first-party API traffic; each task's counts split equally across the occupations sharing the task (weight 1/n occupations). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `equal_1p_api_augmentation_count` | AEI conversations in collaboration modes classed as augmentation (validation + task iteration + learning); first-party API traffic; each task's counts split equally across the occupations sharing the task (weight 1/n occupations). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `equal_claude_ai_task_count` | AEI conversation count for the occupation's tasks; Claude.ai traffic; each task's counts split equally across the occupations sharing the task (weight 1/n occupations). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `equal_claude_ai_automation_count` | AEI conversations in collaboration modes classed as automation (directive + feedback loop); Claude.ai traffic; each task's counts split equally across the occupations sharing the task (weight 1/n occupations). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `equal_claude_ai_augmentation_count` | AEI conversations in collaboration modes classed as augmentation (validation + task iteration + learning); Claude.ai traffic; each task's counts split equally across the occupations sharing the task (weight 1/n occupations). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `equal_total_task_count` | AEI conversation count for the occupation's tasks; both platforms combined; each task's counts split equally across the occupations sharing the task (weight 1/n occupations). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `equal_total_automation_count` | AEI conversations in collaboration modes classed as automation (directive + feedback loop); both platforms combined; each task's counts split equally across the occupations sharing the task (weight 1/n occupations). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `equal_total_augmentation_count` | AEI conversations in collaboration modes classed as augmentation (validation + task iteration + learning); both platforms combined; each task's counts split equally across the occupations sharing the task (weight 1/n occupations). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_1p_api_task_count` | AEI conversation count for the occupation's tasks; first-party API traffic; each task's counts split by employment: the occupation's oews_tot_emp_imputed share among occupations sharing the task (equal split when the task's total employment is not positive). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_1p_api_automation_count` | AEI conversations in collaboration modes classed as automation (directive + feedback loop); first-party API traffic; each task's counts split by employment: the occupation's oews_tot_emp_imputed share among occupations sharing the task (equal split when the task's total employment is not positive). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_1p_api_augmentation_count` | AEI conversations in collaboration modes classed as augmentation (validation + task iteration + learning); first-party API traffic; each task's counts split by employment: the occupation's oews_tot_emp_imputed share among occupations sharing the task (equal split when the task's total employment is not positive). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_claude_ai_task_count` | AEI conversation count for the occupation's tasks; Claude.ai traffic; each task's counts split by employment: the occupation's oews_tot_emp_imputed share among occupations sharing the task (equal split when the task's total employment is not positive). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_claude_ai_automation_count` | AEI conversations in collaboration modes classed as automation (directive + feedback loop); Claude.ai traffic; each task's counts split by employment: the occupation's oews_tot_emp_imputed share among occupations sharing the task (equal split when the task's total employment is not positive). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_claude_ai_augmentation_count` | AEI conversations in collaboration modes classed as augmentation (validation + task iteration + learning); Claude.ai traffic; each task's counts split by employment: the occupation's oews_tot_emp_imputed share among occupations sharing the task (equal split when the task's total employment is not positive). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_total_task_count` | AEI conversation count for the occupation's tasks; both platforms combined; each task's counts split by employment: the occupation's oews_tot_emp_imputed share among occupations sharing the task (equal split when the task's total employment is not positive). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_total_automation_count` | AEI conversations in collaboration modes classed as automation (directive + feedback loop); both platforms combined; each task's counts split by employment: the occupation's oews_tot_emp_imputed share among occupations sharing the task (equal split when the task's total employment is not positive). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_total_augmentation_count` | AEI conversations in collaboration modes classed as augmentation (validation + task iteration + learning); both platforms combined; each task's counts split by employment: the occupation's oews_tot_emp_imputed share among occupations sharing the task (equal split when the task's total employment is not positive). Counts are summed across all cleaned AEI releases; tasks unobserved in AEI contribute zero. |
| `emp_1p_api_task_count_pc` | emp_1p_api_task_count divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is per worker; note that the OpenAI wage analysis reports usage per million workers instead. |
| `emp_1p_api_automation_count_pc` | emp_1p_api_automation_count divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is per worker; note that the OpenAI wage analysis reports usage per million workers instead. |
| `emp_1p_api_augmentation_count_pc` | emp_1p_api_augmentation_count divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is per worker; note that the OpenAI wage analysis reports usage per million workers instead. |
| `emp_claude_ai_task_count_pc` | emp_claude_ai_task_count divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is per worker; note that the OpenAI wage analysis reports usage per million workers instead. |
| `emp_claude_ai_automation_count_pc` | emp_claude_ai_automation_count divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is per worker; note that the OpenAI wage analysis reports usage per million workers instead. |
| `emp_claude_ai_augmentation_count_pc` | emp_claude_ai_augmentation_count divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is per worker; note that the OpenAI wage analysis reports usage per million workers instead. |
| `emp_total_task_count_pc` | emp_total_task_count divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is per worker; note that the OpenAI wage analysis reports usage per million workers instead. |
| `emp_total_automation_count_pc` | emp_total_automation_count divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is per worker; note that the OpenAI wage analysis reports usage per million workers instead. |
| `emp_total_augmentation_count_pc` | emp_total_augmentation_count divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is per worker; note that the OpenAI wage analysis reports usage per million workers instead. |

### `occupations_aei_auto_aug_2025_03_27.csv`

SOC 2010 panel built from the AEI occupation-level automation/augmentation release (2025-03-27), collapsed from 8-digit O*NET-SOC codes.

| Variable | Definition |
| --- | --- |
| `soc_2010` | Six-digit SOC 2010 occupation code (8-digit O*NET-SOC 2010 codes truncated to the parent SOC code). |
| `title_2010` | SOC 2010 occupation title. |
| `group_id` | Connected-component ID of the bipartite SOC 2010<->2018 crosswalk graph; non-singleton groups mark one-to-many or many-to-many crosswalk relationships. |
| `oews_tot_emp_allocated` | OEWS SOC 2018 employment (after the broad-code adjustment) allocated to SOC 2010 by splitting each SOC 2018 code's employment equally across its SOC 2010 crosswalk partners, then summing by SOC 2010. NA when no crosswalk partner has OEWS employment. |
| `oews_tot_emp_imputed` | oews_tot_emp_allocated with missing values filled with the median allocated employment across SOC 2010 occupations. Used as the employment weight and the per-capita denominator. |
| `oews_emp_was_imputed` | True when oews_tot_emp_allocated was missing and oews_tot_emp_imputed holds the median-employment fill value. |
| `oews_a_mean` | OEWS annual mean wage, averaged across the occupation's SOC 2018 crosswalk partners weighted by allocated employment. |
| `pct_occ_scaled` | AEI occupation-level usage share (percent of usage), summed across 8-digit O*NET-SOC sub-occupations. Occupations with O*NET tasks that are absent from the release are recoded to 0; occupations with no O*NET tasks stay NA. |
| `augmentation_weighted_ratio` | AEI augmentation share of classified usage, averaged across sub-occupations weighted by pct_occ_scaled (unweighted mean when the weights sum to zero); same zero-recode rule as pct_occ_scaled. |
| `automation_weighted_ratio` | AEI automation share of classified usage; same aggregation and zero-recode rule as augmentation_weighted_ratio. |
| `pct_occ_scaled_pc` | pct_occ_scaled divided by oews_tot_emp_imputed (zero employment recoded to NA). Scale is share per worker. |

<!-- END CODEBOOK SECTION: occupational_characteristics -->

<!-- BEGIN CODEBOOK SECTION: cross_release_panels -->
## Cross-release panels

Generated by `analysis/cross_release_correlation.py`.

### `cross_release_panel_task.csv`

Wide task-level panel of AEI conversation counts, one row per O*NET task, one column per platform-release.

| Variable | Definition |
| --- | --- |
| `task_key` | O*NET task statement text, lowercased and stripped; the key used to match tasks across releases. |
| `task_count_{platform}_{release}` | Raw AEI conversation count for the unit in the given platform (1p_api or claude_ai) and release (2025_09, 2026_01, or 2026_03). Units unobserved in a release are filled with 0. |
| `task_count_{platform}_pooled` | Sum of the platform's counts across the three releases. |

### `cross_release_panel_occupation.csv`

The task panel aggregated to O*NET-SOC occupations by equal-split apportionment: a task in N occupations contributes 1/N of its count to each.

| Variable | Definition |
| --- | --- |
| `O*NET-SOC Code` | Eight-character O*NET-SOC occupation code. |
| `task_count_{platform}_{release}` | Raw AEI conversation count for the unit in the given platform (1p_api or claude_ai) and release (2025_09, 2026_01, or 2026_03). Units unobserved in a release are filled with 0. |
| `task_count_{platform}_pooled` | Sum of the platform's counts across the three releases. |

<!-- END CODEBOOK SECTION: cross_release_panels -->
