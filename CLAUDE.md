# CLAUDE.md

## Tools

- Always use `uv` to run Python scripts (e.g., `uv run script.py`), never `python` or `python3` directly.
- Run `typst watch docs/eai.typ` in the background to auto-render PDF on save.
